# Code Hygiene Weekly Report — 2026-07-17

## Health Summary
Codebase: **NEEDS ATTENTION**
Security issues: 0 critical | 0 high | 3 medium (test-token literals — false positives, same as prior weeks)
Dead code candidates: 1 true orphan in trading-swarm (write_integration_health.py), 118 in first-repo/scripts (mostly archive)
Duplicate patterns: 3 found (SQLite WAL ×6, Telegram ×4, JSON ×4 — recurring, pending consolidation)
Test coverage gaps: 4 agent-output Python files with no tests (same as last week)
Orphaned worktrees: 0 on disk (1 failed branch `feat/signal-202606042140` still exists in git)
Log directory: trading-swarm 8.6MB (healthy, 15 archived); **first-repo monitoring.log 108MB — OVER THRESHOLD**
Integration contract: **STALE — 5 uncovered commits (2026-07-09 through 2026-07-13)**

---

## 🚨 Log File — OVER THRESHOLD (action required)

**`first-repo/logs/monitoring.log`: 108MB — exceeded the 100MB alert threshold.**

Last week it was 96MB; it has grown another 12MB in 7 days and crossed the limit.
The monitoring service writes to this file continuously. No log rotation is configured.

**Proposal (requires Oscar approval):**
Option A — Rotate now manually:
```bash
cd /home/parison/projects/first-repo
mv logs/monitoring.log logs/monitoring.log.$(date +%Y%m%d)
gzip logs/monitoring.log.$(date +%Y%m%d)
# Service will recreate monitoring.log on next write
```
Option B — Install logrotate config (preferred long-term):
```
/home/parison/projects/first-repo/logs/monitoring.log {
    weekly
    rotate 4
    compress
    missingok
    notifempty
    copytruncate
}
```
At the current rate (+12MB/week), the file will reach 200MB by end of August 2026 without intervention.

Other first-repo logs checked: `daily_maintenance.log` at 28MB (expected), `category_backfill.log` at 6.9MB (can archive — one-off run).

---

## 🔄 Integration Contract Freshness — ACTION REQUIRED

Contract last updated: 2026-07-01 (v2.14 inferred from last date in Section 8).
Five first-repo commits between 2026-07-09 and 2026-07-13 touch schema-critical files with no matching Section 8 entry:

| Hash | Date | File | Subject |
|------|------|------|---------|
| `764839bc` | 2026-07-09 | `scripts/daily_maintenance.py` | fix: O-27 run_step() subprocess timeout |
| `269d8d17` | 2026-07-10 | `scripts/daily_maintenance.py` | feat: O-2 weekly --full-sync backstop |
| `6210bfcc` | 2026-07-10 | `scripts/update_research_exclusions.py` | fix: O-23 durable manual research-exclusion override |
| `c4ac9fad` | 2026-07-10 | `scripts/daily_maintenance.py` | fix: O-26 honest maintenance-completion banner |
| `3371a1af` | 2026-07-13 | `monitoring/database.py` | feat: ELO arc Stage 2 — Writer B canonical formula, atomic write helper |

The `monitoring/database.py` change is the most significant: Writer B now uses a canonical formula and atomic write helper, which changes the ELO write path. Agents querying ELO data need to know what they're getting is from the new atomic writer.

**Action:** Oscar — add entries to `brain/integration-contract.md` Section 8 covering these five commits, particularly:
1. What `monitoring/database.py` atomic write helper means for read consistency
2. Any new/changed columns from the Stage 2 ELO changes
3. Whether `daily_maintenance.py` timeout and backstop changes affect agent scheduling assumptions

A `contract_stale` signal has been written to `brain/signals.json` (status: pending).

---

## 🔒 Security Scan

**trading-swarm:** 0 CRITICAL, 0 HIGH, 1 MEDIUM
- `scripts/polymarket_changelog_monitor.py:45` — `print(f"ERROR: {key} not set...")` — prints a missing env var key *name*, not a credential value. No action needed; this is a benign error message.

**first-repo:** Pattern scanner flagged 3 CRITICAL, but all are false positives upon inspection:
- `scripts/verify_system_observer.py:56,238` — `token='test_token'` — literal placeholder in a verification script, not a real token. Script is in the dead code list (90d inactive).
- `scripts/run_system_observer.py:171` — `telegram_token = "test_token"` — used only in `--no-telegram` test mode; a hardcoded dummy value is correct here.
- `scripts/archive/polygon_wallet_lookup.py:44` — `0x3c499c542cef5e3811e1192ce70d8cc03d5c3359` — the well-known public USDC contract address on Polygon (42-char contract address, not a 32-byte private key). No action needed.

**Verdict: No actionable security issues this week.**

---

## 🗑️ Elon Algorithm Results

Candidates requiring approval before action:

| Score | File | Reasons | Recommendation |
|-------|------|---------|---------------|
| 4 | `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | No registry task, not referenced, 82d inactive | DELETE |
| 4 | `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | No registry task, not referenced, 77d inactive | DELETE |
| 4 | `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | No registry task, not referenced, 82d inactive | DELETE |
| 3 | `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py` | No registry task, not referenced, 52d inactive | ARCHIVE |

Context: These are the raw Python scripts from completed RQ research runs. Their findings are preserved in `brain/findings.json` and research docs — the scripts themselves are execution artifacts. The GEO-ELO-003 file is more recent (52d) so archive is appropriate rather than delete.

**All require Oscar approval before action.** No automatic deletion.

---

## 🧟 Dead Code Candidates

### trading-swarm (true orphans — not referenced from any script, CI, or import)

| File | Days | Recommendation |
|------|------|---------------|
| `scripts/write_integration_health.py` | 64d | Archive — no cron, no import, no caller found |

**Not flagged (active via scripts/CI despite no imports):**
- `ci/validate_backtest.py` — called from `ci/run_ci.sh` ✓
- `orchestrator/context_compressor.py` — called from `scripts/spawn_agent.sh` ✓
- `scripts/run_research_scout.py` — called from `cron_wrappers/run_research_scout.sh` ✓
- `scripts/polymarket_changelog_monitor.py` — called from `cron_wrappers/run_changelog_monitor.sh` ✓
- `scripts/market_filter.py` — imported by `run_feedback_loop_agent.py` ✓

### first-repo (summary)
118 files flagged as dead code candidates (90+ days inactive, not imported).
Predominantly in `scripts/` and `scripts/archive/`. These pre-date the current architecture.
Not actioned here — first-repo is Oscar's domain and the archive subdirectory likely exists intentionally.

---

## 🔄 Duplicate Code

Three patterns duplicated across multiple files (recurring since May 2026):

| Pattern | Files | Priority | Consolidate to |
|---------|-------|----------|---------------|
| SQLite WAL connection | 6 files | HIGH | `orchestrator/shared_utils.py` |
| Telegram message send | 4 files | MEDIUM | `orchestrator/shared_utils.py` |
| JSON file read/write | 4 files | MEDIUM | `orchestrator/shared_utils.py` |

The SQLite WAL pattern appearing in 6 files (up from last check) is the most impactful. Each new quant-research script re-implements the same connection boilerplate.

**Proposal:** Consolidate these into `orchestrator/shared_utils.py`. A full proposed implementation was included in the 2026-05-08 weekly report — still valid. Requires Oscar approval before implementation.

---

## 🧪 Test Coverage Gaps

All four gaps are the same quant-research output scripts as last week (these are also Elon algorithm candidates):

| File | Untested Functions |
|------|-------------------|
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | 7 functions (load_data, collapse_to_implied, elo_weighted_consensus, analyse_market...) |
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | 8 functions (connect_readonly, get_period_split, get_trader_elo, compute_brier_per_trader...) |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | 8 functions (connect_readonly, load_legendary_trader_market_pairs, get_post_entry_price...) |
| `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py` | 8 functions (connect, validate_contract, add_oos_column, compute_oos_elo...) |

If the Elon algorithm DELETE recommendations are approved, these gaps resolve automatically.

---

## 📦 Dependency Audit

**trading-swarm:**
- `requirements.txt` documents: scipy, numpy, pytest, flake8 ✓
- Third-party imports in use: numpy, scipy, pytest — all documented ✓
- False positives (stdlib or internal): fcntl, importlib, multiprocessing (stdlib); market, orchestrator, scripts (internal module paths) — no action needed

**Status: CLEAN**

---

## 🔧 Orphaned Branch

`feat/signal-202606042140` — status `failed` in registry, no worktree on disk, branch still exists in git.

**Proposal (requires approval):** `git branch -D feat/signal-202606042140` — the task failed 42+ days ago and there is no associated worktree or output.

---

## ✅ Maintenance Completed (Automatic)

- **15 log files archived** (>30 days old):
  - feedback-202606052112.log → .gz
  - integration-20260614.log → .gz
  - librarian-20260613.log → .gz
  - hygiene-20260605.log → .gz
  - signal-20260615.log → .gz
  - hygiene-20260612.log → .gz
  - librarian-test-202606061437.log → .gz
  - trader-intel-20260615.log → .gz
  - signal-202606042143.log → .gz
  - analyst-20260615.log → .gz
  - analyst-20260608.log → .gz
  - signal-202606042140.log → .gz
  - integration-20260607.log → .gz
  - signal-20260608.log → .gz
  - librarian-20260606.log → .gz
- **contract_stale signal** written to brain/signals.json

---

## 📋 Proposals Requiring Approval

1. **URGENT — Log rotation for `first-repo/logs/monitoring.log`** (108MB, over threshold)
   - Option A: manual rotate now
   - Option B: logrotate config (recommended)

2. **Elon algorithm — DELETE** `brain/agent-outputs/quant-research/RQ3.2/`, `RQ1.1/`, `RQ2.2/` Python scripts (completed research artifacts, findings preserved elsewhere)

3. **Elon algorithm — ARCHIVE** `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py`

4. **Archive** `scripts/write_integration_health.py` (dead orphan, 64d)

5. **Delete branch** `feat/signal-202606042140` (failed task, 42d old)

6. **Integration contract** — update Section 8 with 2026-07-09 to 2026-07-13 commits (Oscar action)

7. **Shared utils consolidation** — implement `orchestrator/shared_utils.py` to consolidate SQLite WAL, Telegram, JSON patterns (recurring proposal, still unactioned)
