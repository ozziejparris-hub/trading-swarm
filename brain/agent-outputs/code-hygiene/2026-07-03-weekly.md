# Code Hygiene Weekly Report — 2026-07-03

## Health Summary
Codebase: **NEEDS ATTENTION**
Security issues: 0 critical | 0 high | 3 medium (test-token literals in first-repo scripts)
Dead code candidates: 1 flagged (write_integration_health.py — recurring, Monitor)
Duplicate patterns: 3 found (SQLite WAL, Telegram, JSON — recurring, pending consolidation)
Test coverage gaps: 4 agent-output Python files with no tests
Orphaned worktrees: 0 (clean)
Log directory size: first-repo 132MB (monitoring.log 96MB — NEAR THRESHOLD), trading-swarm 8.7MB
Integration contract: **STALE — 2 uncovered commits** (contract_stale signal written)

---

## 🔄 Integration Contract Freshness — ACTION REQUIRED

**Contract latest:** v2.13 (2026-06-23)
**Uncovered commits touching schema-critical files:**

| Hash | Date | File | Subject |
|------|------|------|---------|
| 791dbf5a | 2026-06-25 | `monitoring/database.py` | feat: wire data_source into positions write paths + fix INSERT OR REPLACE bugs |
| c53f9699 | 2026-06-29 | `scripts/daily_maintenance.py` | feat: wire run_tests.py into daily maintenance (non-blocking, file-based results) |

`database.py` is explicitly on the schema-critical watch list. `daily_maintenance.py` is the primary daily maintenance driver.

**Action:** Oscar — add v2.14 to `brain/integration-contract.md` Section 8 covering:
1. `monitoring/database.py` — data_source column additions to positions/markets/trades/traders tables
2. `scripts/daily_maintenance.py` — test runner now wired in (non-blocking, writes `LATEST_TEST_RESULTS.md`)

A `contract_stale` signal has been written to `brain/signals.json` (status: pending).

---

## ⚠️ Log Files — NEAR-CRITICAL

**`first-repo/logs/monitoring.log`: 96MB — 4MB from the 100MB alert threshold.**
This file grows continuously (system writes to it live at 20:02 UTC on the day of this audit). No log rotation is configured. Action needed before next week's audit.

**Recommendation (requires Oscar approval):** Add logrotate config for `monitoring.log` — rotate weekly, compress, keep 4 weeks. Alternatively configure the monitoring service to open the log with `mode='a'` and implement size-based rollover.

Other large first-repo logs:
- `daily_maintenance.log`: 24MB — acceptable, grows daily
- `category_backfill.log`: 6.9MB — one-off backfill, can be archived (run 2026-07-03)
- `sunday_elo.log`: 5.5MB — weekly, acceptable

**Trading-swarm logs: 8.7MB total — healthy.**

**Automated action taken:** Archived 7 agent logs older than 30 days:
- `librarian-20260530.log` → `.log.gz`
- `hygiene-20260529.log` → `.log.gz`
- `signal-20260601.log` → `.log.gz`
- `signal-202606011626.log` → `.log.gz`
- `integration-20260531.log` → `.log.gz`
- `analyst-20260601.log` → `.log.gz`
- `contract-audit-20260602.log` → `.log.gz`

---

## 🔒 Security Issues (Medium — test token literals)

No CRITICAL or HIGH issues found in either repo.

Three MEDIUM findings in `first-repo` — all test/verify scripts, not production code:

| File | Line | Issue |
|------|------|-------|
| `scripts/verify_system_observer.py` | 56, 238 | `token='test_token'` literal in test constructor call |
| `scripts/run_system_observer.py` | 171 | `telegram_token = "test_token"` as fallback default |
| `scripts/archive/test_integration.py` | 95 | `telegram_token='test_token'` in test instantiation |

These are in test/verification scripts, not the monitoring service itself. The monitoring service reads from environment variables. **Low risk — Monitor only.**

---

## 🗑️ Elon Algorithm Results

All 4 quant-research agent output Python files are well-referenced (5–15 references each in docs and brain files). No deletion candidates among agent outputs.

| File | Days stale | Refs | Score | Verdict |
|------|-----------|------|-------|---------|
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | 63 | 15 | 1 | KEEP |
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | 68 | 10 | 1 | KEEP |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | 68 | 9 | 1 | KEEP |
| `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py` | 38 | 5 | 1 | KEEP |

One file of note — `scripts/write_integration_health.py` (50 days stale, no shell caller) — has been flagged in 3 prior weekly reports (2026-05-15, 2026-06-19, 2026-06-26). It IS referenced in integration-contract.md as a required cron step but has no cron_wrapper. **Escalating from Monitor to PROPOSAL REQUIRED.**

**Proposal:** Either add a `cron_wrappers/run_write_integration_health.sh` to schedule it at 06:00 UTC daily (per integration-contract.md Step 15), or confirm it is superseded and mark it as deprecated. Requires Oscar decision.

**Registry cleanup needed:** `signal-202606042140` has been stuck at `status: failed` since 2026-06-04. It is still in `agent_registry.json` as an active_task. This is stale — propose removing it from the registry on next registry maintenance pass.

---

## 🔄 Duplicate Code (Recurring — Pending Consolidation)

No new duplicates introduced this week. The three patterns below have been flagged in prior reports and no consolidation has occurred. Recording for continuity.

| Pattern | Files | Priority | Status |
|---------|-------|----------|--------|
| SQLite WAL connection setup | 8 files in trading-swarm, 50+ in first-repo | MEDIUM | Proposal exists, unapproved |
| Telegram sendMessage | 4 files in trading-swarm | MEDIUM | Proposal exists, unapproved |
| JSON load/dump | 20+ files across both repos | LOW | Common pattern, low priority |

**Note for Oscar:** The `shared_utils.py` proposal from 2026-05-15 remains unimplemented. If consolidation is not a priority for this quarter, consider closing the proposal to reduce hygiene noise.

---

## 🧪 Test Coverage Gaps

No tests exist for any agent-output Python files (research scripts):

| File | Functions | Test needed |
|------|-----------|------------|
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | `run_elo_persistence_test`, `get_elo_quintiles`, `get_brier_scores`, `run_rq1_1` | `tests/test_rq1_1.py` |
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | `run_crowd_vs_elite`, `calculate_market_price_brier`, `get_elite_positions` | `tests/test_rq3_2.py` |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | `analyse_entry_timing`, `get_early_entries` | `tests/test_rq2_2.py` |
| `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py` | `run_oos_validation`, `split_time_periods` | `tests/test_geo_elo_oos.py` |

These are research scripts, not production infrastructure. Priority: LOW unless they are promoted to production use.

---

## 📦 Dependency Audit

**Trading-swarm:** All third-party imports (`numpy`, `scipy`, `pytest`) are documented in `requirements.txt`. `argparse` and `fcntl` are stdlib. `market` and `scripts` are local module references (false positives from grep-based detection). **Clean.**

**First-repo:** All major third-party imports are documented in `requirements.txt`:
- `pandas`, `numpy`, `scipy`, `matplotlib`, `seaborn` — documented
- `requests`, `aiohttp`, `python-telegram-bot` (as `telegram`) — documented
- `apscheduler`, `psutil` — documented
- `pydantic_ai` → maps to `pydantic-ai` — documented
- Local module imports (`database`, `unified_elo_system`, `monitoring`, etc.) — expected, not third-party

**One concern:** `weighted_consensus_system` is imported by `analysis_scheduler.py`, `consensus_divergence_detector.py`, `unified_elo_system.py`, and `market_confidence_meter.py` — but the `priorities.md` notes it is **deprecated**. It still has 5 callers. This is not a dependency issue but a dead-code risk. See Elon Algorithm note.

---

## 🔬 Research Scout Dedup — Ongoing Bug

The pending-review directory has **51 files with significant duplication:**

| Topic | Copies |
|-------|--------|
| when-do-markets-fully-process-public-information | 6 |
| foresightflow-information-leakage-score-framework | 5 |
| polymarket-exchange-upgrade / v2-sdk | 2–3 |
| per-market-information-leakage | 2 |
| anatomy-of-decentralized-prediction-market | 2 |

This is the same dedup bug flagged in memory from 2026-06-16. The research-scout-agent re-files stories it has already seen because it has no dedup check before writing to pending-review. The fix requires adding a content-hash or slug-based check in the research-scout template before writing.

**This has now been flagged for 4 consecutive weeks (2026-06-14 through 2026-07-03) with no fix.** Proposing this be added to Oscar's action list.

---

## ✅ Maintenance Completed

- Worktrees cleaned: 0 orphaned (only `hygiene-20260703` active — current session)
- Logs archived: 7 (trading-swarm agent_logs ≥30 days → `.log.gz`)
- `contract_stale` signal written to `brain/signals.json` (pending)

---

## 📋 Proposals Requiring Oscar's Decision

1. **Integration contract v2.14** — Add Section 8 entry for `database.py` data_source changes (2026-06-25) and `daily_maintenance.py` test-runner wiring (2026-06-29). Straightforward documentation update.

2. **monitoring.log rotation** — `first-repo/logs/monitoring.log` is 96MB and growing live. Add logrotate or in-process rotation before it hits 100MB. **Time-sensitive.**

3. **write_integration_health.py scheduling** — Either add `cron_wrappers/run_write_integration_health.sh` (scheduled 06:00 UTC daily) or formally deprecate. Four-week open item.

4. **Research-scout dedup fix** — Fix in research-scout task template: check for existing slug in pending-review/approved/dismissed before writing a new file. Prevents the 51-file pile-up from growing further.

5. **weighted_consensus_system.py callers** — priorities.md marks this as deprecated and called by Phase 2 scheduler. Confirm it can be safely removed and remove its 5 callers, or re-classify it as supported.

6. **shared_utils.py consolidation** — If duplicate SQLite/Telegram/JSON patterns are still a priority, approve the proposal from 2026-05-15. If not, close the proposal to clean up hygiene noise.
