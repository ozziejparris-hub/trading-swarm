# Code Hygiene Weekly Report — 2026-05-08

## Health Summary
Codebase: **NEEDS ATTENTION** (no critical issues; log bloat and test gaps require action)
Security issues: 0 critical / 0 high / 1 medium (false positive — see below)
Dead code candidates: 0 files
Duplicate patterns: 0 actionable (widespread patterns are expected, not consolidatable)
Test coverage gaps: 3 files (all HIGH priority)
Orphaned worktrees: 4 cleaned
trading-swarm log directory: 2.14 MB (healthy)
first-repo log directory: 482.98 MB (action required)
Integration contract: FRESH

---

## Security Issues

### trading-swarm — 1 MEDIUM (false positive)
`scripts/polymarket_changelog_monitor.py:41`
```
print(f"ERROR: {key} not set. Source ~/.env_trading before running.", file=sys.stderr)
```
**Assessment:** Not a credential leak. The message tells the user which key is missing, not its value. No action needed.

### first-repo — 4 CRITICAL flags (all false positives)

| File | Line | Content |
|------|------|---------|
| `scripts/test_integration.py` | 95 | `SystemObserver(telegram_token='test_token', ...)` |
| `scripts/verify_system_observer.py` | 56 | `TelegramHealthBot(token='test_token', ...)` |
| `scripts/verify_system_observer.py` | 238 | `SystemObserver(telegram_token='test_token', ...)` |
| `scripts/run_system_observer.py` | 171 | `telegram_token = "test_token"` |

**Assessment for run_system_observer.py:171:** This line is inside `if args.no_telegram:` — the test_token only activates when the user explicitly passes `--no-telegram`. This is intentional dummy-mode behaviour, not a production credential. All four findings are false positives.

**Verdict: No CRITICAL escalation required. 0 real security issues found.**

---

## Dead Code Detection

trading-swarm: **0 candidates** — all 12 Python files are either imported, recently modified, or both.

first-repo: Not audited for dead code (209 files; the first-repo codebase is maintained by its own development cycle, not the swarm).

---

## Duplicate Code Patterns

`sqlite3.connect` appears in 86 files across both repos — expected and appropriate. The integration contract (brain/integration-contract.md) governs read-only access patterns from swarm agents; those patterns are correct. Consolidation into a shared_utils.py for first-repo is out of scope for the swarm hygiene agent.

`json.load` appears in 17 files — all appropriate standalone usages.

No Brier score, Telegram sendMessage, or ELO query duplications found within the trading-swarm codebase specifically. The proposed `orchestrator/shared_utils.py` consolidation is not yet warranted — too few instances within swarm code alone.

**No consolidation proposals this week.**

---

## Log File Management

### trading-swarm/logs — HEALTHY
Total: 2.14 MB across 29 files. No files exceed 2 MB. No files older than 30 days. No action needed.

### first-repo/logs — ACTION REQUIRED

| File | Size | Age |
|------|------|-----|
| `elo_recalc_20260426.log` | **292.0 MB** | 12 days |
| `elo_recalc_20260426d.log` | **149.4 MB** | 11 days |
| `monitoring.log` | 32.8 MB | 0 days (active) |
| `elo_recalc_20260426b.log` | 6.6 MB | 12 days |
| `daily_maintenance.log` | 1.2 MB | 0 days (active) |
| others | <0.25 MB each | ≤12 days |

**Total: 483 MB** — dominated by two ELO recalc logs from April 26. These are not yet 30 days old so they do not hit the automatic archive threshold, but at 292 MB and 149 MB they represent significant disk usage. The April 26 recalc appears to have been a large-scale operation that produced unusually verbose output.

**Recommendation (requires Oscar approval):** Compress `elo_recalc_20260426.log` and `elo_recalc_20260426d.log` with gzip now rather than waiting for 30-day threshold. Total compressed size would be approximately 20-40 MB vs 441 MB uncompressed. Command if approved:
```bash
gzip /home/parison/projects/first-repo/logs/elo_recalc_20260426.log
gzip /home/parison/projects/first-repo/logs/elo_recalc_20260426d.log
```

Also note: `monitoring.log` is 32.8 MB and actively growing (daily process). Consider adding logrotate configuration for this file before it becomes a larger problem.

---

## Orphaned Worktree Cleanup

**4 stale git worktree references pruned.** Their working directories had already been deleted by the orchestrator; only the git metadata remained.

Pruned:
- `feat/analyst-20260505` — analyst worktree, directory gone
- `feat/scout-20260504` — scout worktree, directory gone
- `feat/scout-20260505` — scout worktree, directory gone
- `feat/scout-20260506` — scout worktree, directory gone

Active worktrees after cleanup:
```
/home/parison/trading-swarm               [master]
/tmp/.../hygiene-20260508                 [feat/hygiene-20260508]  ← current
/tmp/.../scout-20260507                   [feat/scout-20260507]
/tmp/.../scout-20260508                   [feat/scout-20260508]
```

---

## Test Coverage Gaps

All 3 gaps are in quant-research agent outputs. These scripts contain core statistical functions that feed Phase 5 gate criteria — test coverage is HIGH priority.

| File | Functions Untested | Test File Needed |
|------|-------------------|-----------------|
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | `load_data`, `collapse_to_implied`, `elo_weighted_consensus`, `analyse_market`, `summarise`, `main` | `tests/test_rq3_2_crowd_vs_elite.py` |
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | `connect_readonly`, `get_period_split`, `get_trader_elo`, `compute_brier_per_trader`, `run_pearson`, `classify_group` | `tests/test_rq1_1_elo_persistence.py` |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | `connect_readonly`, `load_legendary_trader_market_pairs`, `get_post_entry_price`, `analyse`, `print_summary`, `main` | `tests/test_rq2_2_entry_timing.py` |

**Note:** These are research analysis scripts that run against the live DB. Unit tests should use small fixture datasets (mock DB or CSV snapshots) to verify the statistical logic in isolation from data availability.

---

## Dependency Audit

trading-swarm `requirements.txt` is clean:

| Category | Packages |
|----------|---------|
| Documented third-party | `numpy`, `scipy`, `pytest`, `flake8` |
| Undocumented (flagged by scan) | `market`, `scripts` — both are **local project modules**, not PyPI packages. False positives. |
| Standard library | Everything else |

No missing requirements.txt entries. No undocumented external dependencies.

---

## Elon Algorithm Audit

3 files audited in `brain/agent-outputs/`. No deletions or archives recommended.

| File | Score | Recommendation | Reasons |
|------|-------|---------------|---------|
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | 2 | KEEP | Not in registry (task pre-dates current registry), not imported (it's a runnable script) |
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | 2 | KEEP | Same — script, not module |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | 2 | KEEP | Same — script, not module |

All three are active, recently modified research scripts that are run directly (not imported). The "not referenced" signal is expected for runnable scripts. The Elon algorithm is not a reliable signal for this file type — these should remain.

---

## Integration Contract Freshness

**FRESH.** Contract Section 8 covers dates: `2026-04-30`, `2026-05-05`, `2026-05-06`, `2026-05-07`.

Recent schema-critical commits in first-repo all have corresponding Section 8 entries. No `contract_stale` signal written.

---

## Known Issue Tracking (from priorities.md)

`weighted_consensus_system.py` is documented as deprecated in priorities.md ("deprecated but still called by Phase 2 scheduler"). Confirmed still imported by:
- `analysis/unified_elo_system.py`
- `analysis/market_confidence_meter.py`
- `analysis/consensus_divergence_detector.py`
- `scripts/run_analysis.py`

This is a first-repo codebase issue, not a trading-swarm issue. Flagging for Oscar's awareness. If the Phase 2 scheduler has been superseded, the import chain should be cleaned up in first-repo to prevent confusion when quant-research-agent reads these files.

---

## Maintenance Completed

- Worktrees pruned: 4 (analyst-20260505, scout-20260504, scout-20260505, scout-20260506)
- Logs archived: 0 (no files exceeded 30-day threshold; large first-repo logs flagged for manual approval)
- Telegram alert sent to agents bot: ✅

---

## Proposals Requiring Approval

### 1. Compress two large ELO recalc logs in first-repo (LOW RISK)
**Files:** `elo_recalc_20260426.log` (292 MB) and `elo_recalc_20260426d.log` (149 MB)
**Action:** `gzip` both files. Saves ~430 MB. Both are 11-12 days old and the recalc operations are complete.
**Why not automatic:** These are in first-repo, not trading-swarm. Hygiene agent defers to Oscar for first-repo modifications.

### 2. Add logrotate for first-repo monitoring.log (MEDIUM EFFORT)
**File:** `monitoring.log` — 32.8 MB today, growing continuously
**Action:** Add `/etc/logrotate.d/polymarket-monitoring` with weekly rotation, 4-week retention, compress
**Why flagged:** Without rotation, this file will reach 200+ MB within weeks. The ELO recalc logs show what happens when large operations run without rotation.

### 3. Write unit tests for 3 quant-research scripts (HIGH PRIORITY)
**Assignee:** quant-research-agent (next task) or Oscar
**Scope:** `tests/test_rq1_1_elo_persistence.py`, `tests/test_rq2_2_entry_timing.py`, `tests/test_rq3_2_crowd_vs_elite.py`
**Why:** These scripts implement Phase 5 gate logic. RQ1.1 and RQ3.2 are stopping-rule questions. Running them without any unit test harness means a logic bug produces silently wrong results.
