# Code Hygiene Weekly Report — 2026-08-28

## Health Summary
Codebase: **NEEDS ATTENTION**
Security issues: 0 critical | 0 high | 1 medium (trading-swarm) + 3 medium (first-repo test placeholders)
Dead code candidates: 12 genuine files (2 false positives corrected)
Duplicate patterns: 2 active patterns (sqlite_connection × 6, telegram_send × 4)
Test coverage gaps: 4 quant-research output files
Orphaned worktrees: 0 (only active hygiene session present)
Log directory size: 10.7 MB — 26 old logs archived to .gz

---

## 🔄 Duplicate Code (MEDIUM priority — proposal required)

### `sqlite_connection` — SQLite WAL connection setup
Found in **6 files** (HIGH: >3 copies):
- `scripts/write_integration_health.py`
- `orchestrator/ollama_agent_loop.py`
- `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py`
- `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py`
- `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py`
- `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py`

Propose consolidating into `orchestrator/shared_utils.py::get_db_connection()`.
**Requires Oscar approval before implementation.**

### `telegram_send` — Telegram message sending
Found in **4 files** (MEDIUM):
- `scripts/run_feedback_loop_agent.py`
- `scripts/polymarket_changelog_monitor.py`
- `orchestrator/ollama_agent_loop.py`
- `orchestrator/orchestrator.py`

`orchestrator.py` already has the canonical implementation. Other callers should import from there or from a shared module.
**Requires Oscar approval before refactoring.**

### `json_read_write` — JSON load/dump
Found in 9 files — 3 are test files (appropriate), 3 already use `orchestrator/json_safety.py`.
`scripts/run_trader_profiling.py`, `scripts/polymarket_changelog_monitor.py`, and `scripts/run_feedback_loop_agent.py` bypass `json_safety.py` for inline read/write.
**Monitor — low urgency given small surface area.**

---

## 🚨 Security Issues

### trading-swarm
**MEDIUM** — `scripts/polymarket_changelog_monitor.py:45`
```
print(f"ERROR: {key} not set. Source ~/.env_trading before running.")
```
This prints an env var **name** (not value) — low real risk. The script itself is
also a dead-code candidate (108 days inactive). No action needed unless script is reactivated.

### first-repo (reclassified from CRITICAL to MEDIUM)
Three files flag `test_token` / `test_chat` literals. All are **explicit test-mode placeholders**,
gated by `--no-telegram` CLI flag or script context:
- `scripts/run_system_observer.py:171-172` — inside `if args.no_telegram:` block
- `scripts/verify_system_observer.py:56,238` — test scaffolding
- `scripts/archive/test_integration.py:95` — archived test file

These are not real credentials. No immediate action required. Consider replacing with
`os.getenv("TELEGRAM_TOKEN", "TEST_PLACEHOLDER")` pattern for clarity on next touch.

---

## 🗑️ Elon Algorithm Results

3 DELETE candidates (score 4/4) — **requires Oscar approval before deletion**:

| File | Score | Reasons |
|------|-------|---------|
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | 4 | No tracked task, not referenced, 124d stale |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | 4 | No tracked task, not referenced, 124d stale |
| `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py` | 4 | No tracked task, not referenced, 94d stale |

These are completed research scripts whose findings have presumably been written to
`findings.json` or session docs. The scripts themselves serve no ongoing purpose.
**Recommend deletion once Oscar confirms findings are captured.**

---

## 🗑️ Dead Code Candidates (Monitor/Archive — requires approval)

### Archive candidates (60+ days stale, not referenced by Python imports)
Note: `ci/validate_backtest.py` and `orchestrator/context_compressor.py` were detected
by import-graph scan but are **shell-invoked** — they are NOT dead. Excluded below.

| File | Days Inactive | Recommendation |
|------|--------------|----------------|
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | 124d | DELETE (Elon) |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | 124d | DELETE (Elon) |
| `scripts/market_filter.py` | 118d | Archive |
| `scripts/polymarket_changelog_monitor.py` | 108d | Archive |
| `scripts/write_integration_health.py` | 106d | Archive |
| `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py` | 94d | DELETE (Elon) |
| `scripts/run_research_scout.py` | 78d | Archive |

### Monitor candidates (30–60 days stale)
| File | Days Inactive | Note |
|------|--------------|------|
| `scripts/run_trader_profiling.py` | 49d | Runner script — may be valid if trader-intel cron is primary path |
| `scripts/run_feedback_loop_agent.py` | 48d | Runner script — check if cron wrapper supersedes it |
| `orchestrator/ollama_agent_loop.py` | 48d | Core orchestrator module — monitor but do not archive |
| `orchestrator/orchestrator.py` | 48d | Core orchestrator — staleness expected (service not started) |
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | 35d | Active research script |

---

## 🧪 Test Coverage Gaps

4 files in `brain/agent-outputs/quant-research/` have no test files:

| File | Untested Functions |
|------|-------------------|
| `RQ3.2/rq3_2_crowd_vs_elite.py` | `load_data`, `collapse_to_implied`, `elo_weighted_consensus`, `analyse_market`, `summarise` |
| `RQ1.1/rq1_1_elo_persistence.py` | `connect_readonly`, `get_period_split`, `get_trader_elo`, `compute_brier_per_trader`, `run_pearson` |
| `RQ2.2/rq2_2_entry_timing.py` | `connect_readonly`, `load_legendary_trader_market_pairs`, `get_post_entry_price`, `analyse`, `print_summary` |
| `GEO-ELO-003/geo_elo_oos_validation.py` | `connect`, `validate_contract`, `add_oos_column`, `compute_oos_elo`, `measure_oos_accuracy` |

**Note:** All 4 are Elon DELETE candidates — test coverage gaps moot if they are deleted.
If retained, tests are needed before any re-use.

---

## 📦 Dependency Audit

**Result: CLEAN** — no undocumented third-party dependencies.

Apparent "undocumented" imports resolved:
- `fcntl`, `importlib`, `multiprocessing` — Python stdlib (Linux)
- `market`, `orchestrator`, `scripts` — internal module references, not PyPI packages

Documented in `requirements.txt`: `scipy>=1.11`, `numpy>=1.26`, `pytest>=8.0`, `flake8>=7.0` ✓

---

## 🔗 Integration Contract Freshness — PASS

Contract has 25 date entries; most recent: `2026-08-01`.
All recent first-repo commits touching schema-critical files
(`daily_maintenance.py`, `unified_elo_system.py`, `database.py`, `update_research_exclusions.py`)
are covered by Section 8 entries. No `contract_stale` signal written.

---

## ✅ Maintenance Completed

- **Logs archived**: 26 old `.log` files compressed to `.gz` (logs older than 30 days)
- **Total log directory size**: 10.7 MB (no files exceeded 100 MB threshold)
- **Worktrees cleaned**: 0 orphans found (only active `hygiene-20260828` session)
- **Signals bus**: 29 total signals, 0 pending — bus clear

---

## 📋 Proposals Requiring Oscar Approval

1. **DELETE** `RQ3.2/rq3_2_crowd_vs_elite.py`, `RQ2.2/rq2_2_entry_timing.py`,
   `GEO-ELO-003/geo_elo_oos_validation.py` — Elon algorithm score 4/4.
   Confirm findings captured in `findings.json` / session docs before deletion.

2. **Archive** `scripts/market_filter.py`, `scripts/polymarket_changelog_monitor.py`,
   `scripts/write_integration_health.py`, `scripts/run_research_scout.py`
   — all 78–118 days inactive, not referenced.

3. **Consolidate** SQLite WAL connection setup into `orchestrator/shared_utils.py::get_db_connection()`.
   Affects 6 files. Reduces drift risk as more agents come online.

4. **Consolidate** Telegram send into a single shared function (import from `orchestrator.py`
   or new `shared_utils.py`). Currently in 4 files.

---

## first-repo Audit Note

first-repo has **178 dead-code candidates** by import-graph scan — typical for a mature
monitoring repo with many one-off diagnostic scripts. The top 10 are all 132+ days stale
diagnostic/utility scripts in `scripts/`. This is expected accumulation.

No action recommended this week — the scripts in `scripts/archive/` and standalone
diagnostics are low-risk. Recommend a dedicated first-repo hygiene pass when the trading
swarm moves to Phase 6 and the two repos are formally integrated.
