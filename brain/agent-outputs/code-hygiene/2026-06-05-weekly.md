# Code Hygiene Weekly Report — 2026-06-05

## Health Summary
Codebase: **CLEAN** — no real security issues, entropy stable
Security issues: 0 critical, 0 high, 0 medium (all hits are unchanged false positives)
Dead code candidates: 5 MONITOR (trading-swarm), 125 MONITOR (first-repo)
Duplicate patterns: 3 (unchanged — shared_utils.py proposal still pending)
Test coverage gaps: 4 agent output files (unchanged)
Orphaned worktrees: 1 pruned (signal-202606042140 stale git reference removed)
Logs archived: 11 (trading-swarm); first-repo monitoring.log at 67MB (trending up, watch)

---

## ✅ Maintenance Completed

### Log Archiving — 11 files archived (trading-swarm)
The following logs exceeded the 30-day threshold and were gzip-compressed in place:
- `logs/agent_logs/analyst-20260505.log`
- `logs/agent_logs/scout-20260501.log`
- `logs/agent_logs/scout-20260505.log`
- `logs/agent_logs/librarian-20260502-2.log`
- `logs/agent_logs/scout-20260504.log`
- `logs/agent_logs/librarian-20260502.log`
- `logs/agent_logs/librarian-test.log`
- `logs/agent_logs/scout-20260502.log`
- `logs/agent_logs/scout-20260503.log`
- `logs/agent_logs/scout-20260430.log`
- `logs/agent_logs/hygiene-20260501.log`

Trading-swarm log directory: 6.94 MB total. No files exceed 100 MB threshold.

### Git Worktree Prune — 1 stale reference removed
`git worktree prune` removed stale entry for `signal-202606042140`:
```
Removing worktrees/signal-202606042140: gitdir file points to non-existent location
```
The worktree directory was empty and the gitdir reference was broken. Registry still shows this task as `respawning` — see Oscar action item below.

---

## 🚨 Security Scan

### trading-swarm — CLEAN
**1 scanner hit, 0 real issues.**
- `scripts/polymarket_changelog_monitor.py:45` — MEDIUM: prints the *name* of a missing env var (`key` is the variable name, not a credential value). FALSE POSITIVE. Unchanged from previous weeks.

### first-repo — CLEAN (all flagged items are false positives)
**5 CRITICAL scanner hits, 0 real issues.** Identical to last 3 weeks:

| File | Line | Finding | Verdict |
|------|------|---------|---------|
| `scripts/test_integration.py:95` | `telegram_token='test_token'` | Test/diagnostic script — dummy value | FALSE POSITIVE |
| `scripts/verify_system_observer.py:56` | `token='test_token'` | Diagnostic script | FALSE POSITIVE |
| `scripts/verify_system_observer.py:238` | `telegram_token='test_token'` | Same | FALSE POSITIVE |
| `scripts/run_system_observer.py:171` | `telegram_token = "test_token"` | Reached only under `--no-telegram` test mode | FALSE POSITIVE |
| `scripts/polygon_wallet_lookup.py:44` | `USDC_POLYGON = "0x3c499c..."` | Well-known public USDC contract address on Polygon — not a private key | FALSE POSITIVE |

MEDIUM hits: 97 (same false-positive flood from `print_credentials` regex matching benign `key`/`token` in error messages).

**Standing recommendation (4th week):** Tighten pattern to `print.*(?:token|key)\s*[:=]\s*\S+` to require an actual value being printed.

---

## 📋 Oscar Action Items

### 1. Signal task stuck in `respawning` — registry cleanup needed
`signal-202606042140` shows `status: respawning` in `orchestrator/agent_registry.json` but:
- Tmux session `signal-agent-signal-202606042140` does NOT exist
- Worktree directory was empty (pruned as stale reference above)
- The signals it was spawned to detect (STR003-005, STR003-006 for Peru election) were already written and are `processed` in `signals.json`

**Recommendation:** Mark the task `done` or `failed` in `agent_registry.json` and close it out. The work succeeded (signals were written) even though the agent session died before completing cleanly.

### 2. first-repo monitoring.log trending toward 100 MB threshold
- Current: **67 MB** (up from 60.5 MB last week — growing ~6.5 MB/week)
- daily_maintenance.log: **19 MB** (was 98.8 MB last week — clearly rotated)
- At current growth rate, monitoring.log will breach 100 MB in **~5 weeks**
- **Recommendation:** Confirm log rotation is configured for `monitoring.log`. The daily_maintenance.log rotation behaviour suggests one is already in place for that file.

---

## 🗑️ Elon Algorithm Results

4 candidates, all scoring 3 or below — no DELETE recommendations.

| File | Score | Recommendation | Last Modified | Reasons |
|------|-------|---------------|---------------|---------|
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | 3 | ARCHIVE | 2026-04-26 | No task in registry; not referenced; 40d stale |
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | 3 | ARCHIVE | 2026-05-01 | No task in registry; not referenced; 35d stale |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | 3 | ARCHIVE | 2026-04-26 | No task in registry; not referenced; 40d stale |
| `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py` | 2 | MONITOR | 2026-05-26 | No task in registry; not referenced |

**Context note:** RQ1.1, RQ3.2, and RQ2.2 are completed research runs — their findings are documented in `brain/findings.json` and strategy notes. The scripts themselves are research artifacts (evidence files), not active code. The "no task in registry" flags are expected since these tasks predate or were not tracked in the current registry format. **Recommend keeping all 4 until Phase 2 First Light is complete** — they may be re-run for comparison.

No Elon algorithm deletion candidates this week (no file scored 4+). Codebase entropy is stable.

---

## 🔄 Duplicate Code

3 patterns flagged — unchanged for 4 consecutive weeks. Shared_utils.py proposal remains pending.

| Pattern | Priority | Files | Notes |
|---------|----------|-------|-------|
| `sqlite_connection` (WAL mode) | HIGH | 7 files in trading-swarm, 41 in first-repo | Consolidation proposal from 2026-05-08 still awaiting Oscar approval |
| `telegram_send` | HIGH | 4 files in trading-swarm | Orchestrator, ollama_agent_loop, run_feedback_loop_agent, polymarket_changelog_monitor |
| `json_read_write` | HIGH | 7 files in trading-swarm, 8 in first-repo | |

**No new duplicates found this week.** Consolidation is not urgent but the proposal has been pending for 4 weeks. If shared_utils.py is not going to be implemented, it should be explicitly rejected so this finding no longer appears.

---

## 🧪 Test Coverage Gaps

4 agent output files remain without tests — unchanged from last week.

| File | Priority | Functions without tests |
|------|----------|------------------------|
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | HIGH | `load_data()`, `collapse_to_implied()`, `elo_weighted_consensus()`, `analyse_market()`, `summarise()`, `main()`, `print_market_table()` |
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | HIGH | `connect_readonly()`, `get_period_split()`, `get_trader_elo()`, `compute_brier_per_trader()`, `run_pearson()`, `classify_group()`, `verdict()`, `main()` |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | HIGH | `connect_readonly()`, `load_legendary_trader_market_pairs()`, `get_post_entry_price()`, `analyse()`, `print_summary()`, `main()`, `tier_stats()`, `dir_stats()` |
| `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py` | HIGH | `connect()`, `validate_contract()`, `add_oos_column()`, `compute_oos_elo()`, `measure_oos_accuracy()`, `get_oos_distribution()`, `main()`, `tier()` |

Note: These are analysis scripts, not production utilities. Tests would verify statistical logic but have low priority vs. other system needs.

---

## 📦 Dependency Audit

### trading-swarm
Requirements file: `requirements.txt`  
Third-party imports detected: `numpy`, `pytest`, `scipy`  
Undocumented: `market`, `scripts` — both are local modules (false positives), not PyPI packages.  
**Status: CLEAN**

### first-repo
Requirements file: `requirements.txt`

**2 genuinely undocumented third-party packages:**

| Import | Package | Status |
|--------|---------|--------|
| `apscheduler` | `APScheduler` | Used in `analysis/analysis_scheduler.py` — NOT in requirements.txt |
| `psutil` | `psutil` | Used in monitoring — NOT in requirements.txt |

All other "undocumented" hits (`analysis`, `database`, `monitoring`, `paper_trading`, `detect_insider_activity`, `unified_elo_system`, `weighted_consensus_system`, `trader_specialization_analysis`) are local module imports, not PyPI packages.

`dotenv` → covered by `python-dotenv`, `pydantic_ai` → covered by `pydantic-ai`, `telegram` → covered by `python-telegram-bot`, `difflib` → stdlib.

**Recommendation:** Add to first-repo `requirements.txt`:
```
APScheduler>=3.10.0
psutil>=5.9.0
```

---

## 🕰️ Integration Contract Freshness

**FRESH** — `brain/integration-contract.md` Section 8 covers all recent schema-critical commits.

Contract change-log dates found: 2026-04-30, 2026-05-05, 2026-05-06, 2026-05-07, 2026-05-08, 2026-05-14, 2026-05-20, 2026-05-25, 2026-05-29, 2026-06-02, 2026-06-05.

Recent schema-critical first-repo commits (last 10):
- `fb380f4 fix: fast_resolution_check.py — add CLOB stale market second pass`
- `57eb59b fix: apply_full_elo_modifiers executemany + positions composite index`
- `6d2896c fix: tighten Pattern B arb detection threshold 0.05→0.03`
- `d7a961b feat: flag 2 Pattern B ARB_BOTs — symmetric arb detection`
- `631a2f6 feat: automated ARB_BOT detection — patterns A/B/C daily maintenance`
- `b5967d4 fix: lazy-init CopyTradeDetector — prevent O(n²) correlation matrix`

None of these touch `daily_maintenance.py`, `unified_elo_system.py`, `database.py`, or `update_research_exclusions.py`. Contract is current. No `contract_stale` signal written.

---

## 🔍 Dead Code Detection

### trading-swarm (5 MONITOR)
| File | Days Stale | Notes |
|------|-----------|-------|
| `scripts/market_filter.py` | 34d | One-off utility, not imported |
| `ci/validate_backtest.py` | 48d | **FALSE POSITIVE** — CI runner script, executed directly not imported |
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | 40d | Research artifact (completed) |
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | 35d | Research artifact (completed) |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | 40d | Research artifact (completed) |

`ci/validate_backtest.py` is an executor script, not a library — the dead code scanner cannot detect it's run by the CI pipeline. Not dead.

### first-repo (125 MONITOR)
Predominantly one-off backfill scripts (`backfill_*.py`), diagnostic scripts (`diagnose_*.py`, `check_*.py`), and utility scripts that were run once. Character unchanged from previous weeks — this is a scripts-heavy repo where ad-hoc scripts accumulate. No Archive-tier candidates (all under 60 days or recently used directly).
