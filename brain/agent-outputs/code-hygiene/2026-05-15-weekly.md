# Code Hygiene Weekly Report — 2026-05-15

## Health Summary
Codebase: **NEEDS ATTENTION** (no security issues; 16 orphaned worktrees and persistent test gaps require action)
Security issues: 0 critical / 0 high / 1 medium (false positive)
Dead code candidates: 0 files
Duplicate patterns: 5 patterns found (all expected; 1 consolidation opportunity)
Test coverage gaps: 3 files (HIGH — same 3 as last 2 weeks, unaddressed)
Orphaned worktrees: 16 identified (all clean, requiring Oscar approval to remove)
trading-swarm log directory: 4.00 MB (healthy)
first-repo log directory: 495 MB (still growing — ELO recalc logs uncompressed for 2nd week)
Integration contract: FRESH — Section 8 covers through 2026-05-14

---

## Security Issues

### trading-swarm — 1 MEDIUM (false positive)
`scripts/polymarket_changelog_monitor.py:45`
```
print(f"ERROR: {key} not set. Source ~/.env_trading before running.", file=sys.stderr)
```
**Assessment:** Not a credential leak. Tells the user which env var is missing, not its value. No action needed.

### first-repo — 4 CRITICAL flags (all confirmed false positives, same as last week)

| File | Line | Content |
|------|------|---------|
| `scripts/test_integration.py` | 95 | `SystemObserver(telegram_token='test_token', ...)` |
| `scripts/verify_system_observer.py` | 56 | `TelegramHealthBot(token='test_token', ...)` |
| `scripts/verify_system_observer.py` | 238 | `SystemObserver(telegram_token='test_token', ...)` |
| `scripts/run_system_observer.py` | 171 | `telegram_token = "test_token"` |

**Assessment:** All confirmed false positives. The `run_system_observer.py:171` value is inside `if args.no_telegram:` — it only activates when the user explicitly passes `--no-telegram`. Test scripts use dummy tokens by design. **No CRITICAL escalation required. 0 real security issues.**

The first-repo MEDIUM count (97 findings) is almost entirely false positives from `print("Key findings:")`, `print(f"Available keys: ...")`, and similar benign output — not credential leaks.

**Verdict: Codebase is clean. No security action required.**

---

## Elon Algorithm Results

3 files in `brain/agent-outputs/` scored 2/5. **No deletions or archives recommended** — same verdict as the past two weeks.

| File | Score | Recommendation | Reason |
|------|-------|---------------|---------|
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | 2 | **KEEP** | Active research script; runs directly (not imported). "Not referenced" is expected for runnable scripts. |
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | 2 | **KEEP** | Same — Phase 5 stopping-rule question. |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | 2 | **KEEP** | Same — active research pipeline. |

All three are Phase 5 gate scripts. Elon algorithm is not a reliable signal for standalone runnable scripts — the "not imported" flag is expected by design. **No action.**

---

## Orphaned Worktrees — Action Required

16 orphaned worktrees found in `/tmp/trading-swarm-worktrees/`. All have **0 uncommitted changes**. Total disk usage: **28 MB**.

These accumulated from agents that ran this week (analyst, backtest, integration, librarian, quant, scouts, signal). The registry only tracks 2 currently running tasks.

| Worktree | Last Commit | Status |
|----------|-------------|--------|
| `analyst-20260511` | feat/analyst-20260511 | Orphaned |
| `analyst-20260513` | feat/analyst-20260513 | Orphaned |
| `backtest-20260513` | feat/backtest-20260513 | Orphaned |
| `hygiene-20260508` | feat/hygiene-20260508 | Orphaned |
| `integration-20260510` | feat/integration-20260510 | Orphaned |
| `integration-20260512` | feat/integration-20260512 | Orphaned |
| `librarian-20260509` | feat/librarian-20260509 | Orphaned |
| `quant-20260513` | feat/quant-20260513 | Orphaned |
| `scout-20260507` through `scout-20260513` (7 worktrees) | Various | Orphaned |
| `signal-20260513` | feat/signal-20260513 | Orphaned |

**Requires Oscar approval.** Cleanup command when approved:
```bash
for wt in analyst-20260511 analyst-20260513 backtest-20260513 hygiene-20260508 \
           integration-20260510 integration-20260512 librarian-20260509 quant-20260513 \
           scout-20260507 scout-20260508 scout-20260509 scout-20260510 scout-20260511 \
           scout-20260512 scout-20260513 signal-20260513; do
    git -C /home/parison/trading-swarm worktree remove /tmp/trading-swarm-worktrees/$wt --force
done
git -C /home/parison/trading-swarm worktree prune
```

**Note:** The orchestrator should auto-remove worktrees on task completion. If it is not doing this, consider adding cleanup to the task completion path in `orchestrator/orchestrator.py`.

---

## Duplicate Code

5 patterns found across trading-swarm Python files. All expected given the architecture except one:

| Pattern | Files | Priority | Assessment |
|---------|-------|----------|------------|
| `json.load / json.dump` | 11 files | — | Expected. Standard file I/O. |
| `sqlite3.connect` | 6 files | — | Expected. Agent output scripts connect independently per the integration contract. |
| `json read/write` | 11 files | — | Expected. |
| `brier_score calculation` | 5 files | MEDIUM | 3 agent scripts + validate_backtest.py + orchestrator.py |
| `elo_query` | 5 files | MEDIUM | 3 agent scripts + run_feedback_loop_agent.py + write_integration_health.py |

**Consolidation opportunity:** The Brier score calculation and ELO query patterns each appear in 5 files. A `orchestrator/shared_utils.py` is warranted now that the pattern count crosses the threshold. However, the agent output scripts (RQ1.1, RQ2.2, RQ3.2) are designed as standalone scripts — they should stay self-contained. Consolidation target is the infra scripts only.

**Proposal: Add `calculate_brier_score()` and `get_trader_elo_scores()` to `orchestrator/shared_utils.py`** — see Proposals section below.

---

## Test Coverage Gaps

Same 3 gaps as the past 2 weeks. These have not been addressed. **Escalating priority.**

| File | Functions Without Tests | Test File Needed |
|------|------------------------|-----------------|
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | `load_data`, `collapse_to_implied`, `elo_weighted_consensus`, `analyse_market`, `summarise`, `main` | `tests/test_rq3_2_crowd_vs_elite.py` |
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | `connect_readonly`, `get_period_split`, `get_trader_elo`, `compute_brier_per_trader`, `run_pearson`, `classify_group` | `tests/test_rq1_1_elo_persistence.py` |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | `connect_readonly`, `load_legendary_trader_market_pairs`, `get_post_entry_price`, `analyse`, `print_summary`, `main` | `tests/test_rq2_2_entry_timing.py` |

RQ1.1 and RQ3.2 are **Phase 5 stopping-rule questions**. Running them against the live DB without any unit test harness means a logic bug produces silently wrong results that could pass or fail Phase 5 gates incorrectly. This has been flagged 3 weeks in a row. It should be assigned explicitly.

**Recommended assignee:** quant-research-agent next task, or Oscar manually. Tests should use small fixture datasets (mock SQLite DB with 10-20 rows) to verify statistical logic without live DB dependency.

---

## Dependency Audit

trading-swarm `requirements.txt` remains clean.

| Category | Packages |
|----------|---------|
| Documented third-party | `numpy`, `scipy`, `pytest`, `flake8` |
| Scanner flags (false positives) | `argparse`, `fcntl` — Python stdlib. `market`, `scripts` — internal project modules. |
| Missing from requirements | None |

No action required.

---

## Log File Management

### trading-swarm/logs — HEALTHY
4.00 MB across all log files. No file exceeds 2 MB. No files older than 30 days. No action needed.

### first-repo/logs — SECOND WEEK FLAGGED (495 MB, growing)

| File | Size | Age | Status |
|------|------|-----|--------|
| `elo_recalc_20260426.log` | **292 MB** | 19 days | Compressible — awaiting Oscar approval (2nd week) |
| `elo_recalc_20260426d.log` | **149 MB** | 18 days | Compressible — awaiting Oscar approval (2nd week) |
| `monitoring.log` | 44 MB | 0 days (active) | Growing; recommend logrotate |
| `elo_recalc_20260426b.log` | 6.6 MB | 19 days | Minor |
| Others | <1 MB each | ≤19 days | Fine |

Total: 495 MB (+12 MB vs last week from monitoring.log growth).

The two ELO recalc logs hit 30-day threshold in ~11 days (2026-05-26), at which point they qualify for automatic archiving. Oscar can approve early compression now to recover ~430 MB.

---

## Dead Code Detection

trading-swarm: **0 candidates** — all 14 Python files are recently modified or actively imported.

first-repo: Not audited for dead code (212 files; maintained by its own development cycle).

**Known dead code in first-repo (from priorities.md, persistent):**
`analysis/weighted_consensus_system.py` is documented as deprecated but still imported by:
- `analysis/unified_elo_system.py`
- `analysis/market_confidence_meter.py`
- `analysis/consensus_divergence_detector.py`
- `scripts/run_analysis.py`

This import chain should be cleaned up when the Phase 2 scheduler is confirmed superseded. Out of scope for swarm hygiene agent to fix — flagged for Oscar.

---

## Integration Contract Freshness

**FRESH.** Contract Section 8 covers dates: `2026-04-30`, `2026-05-05`, `2026-05-06`, `2026-05-07`, `2026-05-14`.

Checked last 40 commits in first-repo against schema-critical files (`daily_maintenance.py`, `unified_elo_system.py`, `database.py`, `update_research_exclusions.py`). Zero uncovered schema-critical commits. No `contract_stale` signal written.

---

## Maintenance Completed

- Worktrees cleaned: **0** (16 identified, cleanup pending Oscar approval — all clean)
- Logs archived: **0** (no files hit 30-day threshold in trading-swarm; first-repo logs pending approval)
- Integration contract: checked, FRESH
- Telegram alert: sent to agents bot

---

## Proposals Requiring Approval

### 1. Remove 16 orphaned worktrees (LOW RISK, SAFE)
**Why:** All 16 have 0 uncommitted changes. They are leftover from completed agent tasks this week. Cleanup command provided above.
**Risk:** None — all work is committed to git branches.
**Recommended action:** Oscar runs the cleanup command above, or it is added to the orchestrator task completion path.

### 2. Compress two large ELO recalc logs in first-repo (2nd week — LOW RISK)
**Files:** `elo_recalc_20260426.log` (292 MB) and `elo_recalc_20260426d.log` (149 MB)
**Action:** `gzip /home/parison/projects/first-repo/logs/elo_recalc_20260426.log && gzip /home/parison/projects/first-repo/logs/elo_recalc_20260426d.log`
**Saves:** ~430 MB. Both files are complete (ELO recalc finished 19 days ago).
**Why not automatic:** These are in first-repo; hygiene agent defers first-repo modifications to Oscar.

### 3. Add logrotate for first-repo monitoring.log (2nd week — MEDIUM EFFORT)
**File:** `monitoring.log` at 44 MB today, growing at ~6 MB/week.
**Action:** Add `/etc/logrotate.d/polymarket-monitoring` config with weekly rotation, 4-week retention, compress.
**Why:** Will reach 200+ MB within a month without rotation.

### 4. Assign test-writing task for 3 quant-research scripts (HIGH PRIORITY — 3rd week flagged)
**Assignee:** quant-research-agent or Oscar
**Files:** `tests/test_rq1_1_elo_persistence.py`, `tests/test_rq2_2_entry_timing.py`, `tests/test_rq3_2_crowd_vs_elite.py`
**Why:** RQ1.1 and RQ3.2 are Phase 5 stopping-rule questions. Running them untested against live DB risks silently wrong Phase 5 gate results. Third consecutive week unflagged — recommend explicit assignment.

### 5. Create orchestrator/shared_utils.py (MEDIUM EFFORT)
**Scope:** Extract `calculate_brier_score()` and `get_trader_elo_scores()` — these now appear in 5 files each within the infra layer (excluding standalone agent scripts).
**Target files to update after creation:** `scripts/run_feedback_loop_agent.py`, `scripts/write_integration_health.py`, `orchestrator/orchestrator.py`, `ci/validate_backtest.py`
**Why now:** Threshold of 3+ infra files with identical pattern is crossed. Agent output scripts (RQ1.1, RQ2.2, RQ3.2) should remain standalone.

### 6. Add orchestrator worktree cleanup on task completion
**Why:** 16 orphaned worktrees accumulated in 1 week. The orchestrator should call `git worktree remove` when it marks a task `done` or `failed`. This would eliminate the manual cleanup need entirely.
**File to modify:** `orchestrator/orchestrator.py` — add to the task completion handler.
**Effort:** Low — 5-10 lines in the existing task completion path.
