# Code Hygiene Weekly Report — 2026-05-29

## Health Summary
Codebase: **CLEAN** — no real security issues, codebase entropy stable
Security issues: 0 critical, 0 high, 0 medium (all scanner hits are false positives — details below)
Dead code candidates: 2 MONITOR (trading-swarm), 44 MONITOR (first-repo)
Duplicate patterns: 3 (unchanged from last week — shared_utils.py proposal still pending)
Test coverage gaps: 4 agent output files (1 new: GEO-ELO-003)
Orphaned worktrees: 8 remain (5 merged, flagged for removal)
Logs archived: 7 (trading-swarm)
First-repo log alert: daily_maintenance.log at 98.8MB, monitoring.log at 60.5MB

---

## ✅ Maintenance Completed

### Log Archiving — 7 files archived
The following logs exceeded the 30-day threshold and were gzip-compressed in place:
- `logs/agent_logs/test-001.log` (32d, 0.04MB)
- `logs/agent_logs/scout-20260427.log` (32d, 0.04MB)
- `logs/agent_logs/scout-20260428.log` (31d, 0.04MB)
- `logs/agent_logs/scout-20260429.log` (30d, 0.04MB)
- `logs/agent_logs/signal-001.log` (38d, 0.03MB)
- `logs/agent_logs/signal-002.log` (32d, 0.03MB)
- `logs/agent_logs/backtest-str001.log` (32d, 0.03MB)

Log directory now 6.13MB total (all current). No logs exceed the 100MB threshold.

### Git Worktree Prune
`git worktree prune` ran successfully — no stale references (all worktree directories still exist).

---

## 🔒 Security Scan

### trading-swarm — CLEAN
**1 scanner hit, 0 real issues.**
- `scripts/polymarket_changelog_monitor.py:45` — MEDIUM: prints the *name* of a missing env var
  (`key` is the variable name, not a credential value). FALSE POSITIVE.

### first-repo — CLEAN (all flagged items are false positives)
**4 CRITICAL scanner hits, 0 real issues.**

| File | Line | Finding | Verdict |
|------|------|---------|---------|
| `scripts/test_integration.py:95` | `telegram_token='test_token'` | Test/diagnostic script only — dummy value, never reaches production | FALSE POSITIVE |
| `scripts/verify_system_observer.py:56` | `token='test_token'` | Verify/diagnostic script only | FALSE POSITIVE |
| `scripts/verify_system_observer.py:238` | `telegram_token='test_token'` | Same | FALSE POSITIVE |
| `scripts/run_system_observer.py:171` | `telegram_token = "test_token"` | Only reached under `--no-telegram` flag (explicit test mode) | FALSE POSITIVE |
| `scripts/polygon_wallet_lookup.py:44` | `USDC_POLYGON = "0x3c499c..."` | Well-known public USDC contract address on Polygon — not a private key | FALSE POSITIVE |

The ~100 MEDIUM hits across first-repo are all false positives from the `print_credentials` regex
matching benign uses of "key", "keys", "token" in error/help messages. This has been reported three
consecutive weeks. The scanner's `print_credentials` pattern remains too broad for first-repo's
diagnostic script density.

**Standing recommendation:** Tighten pattern to `print.*(?:token|key)\s*[:=]\s*\S+` to require
an actual value being printed. Eliminates false positive flood without losing true detection.

---

## ⚠️ First-Repo Log Alert (Oscar action needed)

`/home/parison/projects/first-repo/logs/` is at **159MB total**:
- `daily_maintenance.log`: **98.8MB** — approaching the 100MB threshold
- `monitoring.log`: **60.5MB**

These are live monitoring system logs — not safe to archive automatically.
**Oscar: please rotate these manually or configure logrotate for the monitoring service.**

Suggested command (keeps last 30 days):
```bash
# After stopping/restarting monitoring service:
gzip -c /home/parison/projects/first-repo/logs/daily_maintenance.log > /home/parison/projects/first-repo/logs/daily_maintenance-$(date +%Y-%m-%d).log.gz
: > /home/parison/projects/first-repo/logs/daily_maintenance.log
```

---

## ⚠️ Worktrees Requiring Oscar Review

8 live worktrees remain (excluding current session). 5 have branches already merged to master
and are safe to remove:

### Safe to Remove (branches merged to master)
```bash
git -C /home/parison/trading-swarm worktree remove /tmp/trading-swarm-worktrees/hygiene-20260522 --force
git -C /home/parison/trading-swarm worktree remove /tmp/trading-swarm-worktrees/integration-20260524 --force
git -C /home/parison/trading-swarm worktree remove /tmp/trading-swarm-worktrees/librarian-20260523 --force
git -C /home/parison/trading-swarm worktree remove /tmp/trading-swarm-worktrees/signal-20260525 --force
git -C /home/parison/trading-swarm worktree remove /tmp/trading-swarm-worktrees/geo-elo-003-20260526 --force
```

### Flagged for Investigation (branches not merged to master)
| Worktree | Branch | Last Commit | Notes |
|----------|--------|------------|-------|
| `analyst-20260525` | `feat/analyst-20260525` | `docs: performance-analyst weekly run 2026-05-25` | 4 days old, not merged |
| `backtest-lh001-v4-20260522` | `feat/backtest-lh001-v4-20260522` | `research: LH-001 backtest validation v2 complete` | **Flagged last week too** — 7 days old, still not merged or removed |
| `handoff-test-t3-20260526` | `feat/handoff-test-t3-20260526` | `test: Tier 3 handoff-mode strategic assessment` | 3 days old, test branch |

`backtest-lh001-v4-20260522` has been unresolved for 2 consecutive weeks. Please decide:
merge the backtest work to master, or remove the worktree if the validation is superseded.

---

## 🔄 Duplicate Code (Proposals — Oscar approval still pending)

Same 3 patterns as last two weeks — no change, shared_utils.py not yet created.

| Pattern | Files | Priority |
|---------|-------|----------|
| SQLite WAL connection setup | 7 (trading-swarm) + 34 (first-repo) = 41 total | HIGH |
| Telegram sendMessage | 4 (trading-swarm) | MEDIUM |
| json.load/dump with error handling | 9 (trading-swarm) + 14 (first-repo) = 23 total | MEDIUM |

**Reminder:** `orchestrator/shared_utils.py` was proposed 2 weeks ago. Consolidating sqlite_wal
and json_load_dump would reduce the surface area for bugs in every new agent output that queries
the database. Recommend creating during Phase 2 prep.

**`weighted_consensus_system.py` status update (improvement):**
`analysis/analysis_scheduler.py` no longer imports `weighted_consensus_system`. This removes the
Phase 2 scheduler dependency that was flagged in `priorities.md`. However 4 other files still
reference it (`market_confidence_meter.py`, `unified_elo_system.py`, `consensus_divergence_detector.py`,
`run_analysis.py`). The module is deprecated but not dead — deprecation work is progressing.

---

## 🧪 Test Coverage Gaps

| File | Functions Without Tests | Test File Needed | Priority |
|------|------------------------|-----------------|----------|
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | `load_data`, `collapse_to_implied`, `elo_weighted_consensus`, `analyse_market`, `summarise` | `tests/test_rq3_2_crowd_vs_elite.py` | MEDIUM |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | `connect_readonly`, `load_legendary_trader_market_pairs`, `get_post_entry_price`, `analyse`, `print_summary` | `tests/test_rq2_2_entry_timing.py` | MEDIUM |
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | `connect_readonly`, `get_period_split`, `get_trader_elo`, `compute_brier_per_trader`, `run_pearson` | `tests/test_rq1_1_elo_persistence.py` | MEDIUM |
| `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py` | `connect`, `validate_contract`, `add_oos_column`, `compute_oos_elo`, `measure_oos_accuracy` | `tests/test_geo_elo_oos_validation.py` | MEDIUM (new this week) |

Note: GEO-ELO-003 is 3 days old — added since last week's report. These are all database-dependent
research scripts. Snapshot/regression tests on output format are the practical approach (not pure
unit tests). Recommend when next updated.

---

## 📦 Dependency Audit

**trading-swarm — CLEAN**
Scanner reports 3 apparent "undocumented" imports — all are false positives:
- `fcntl` — Linux standard library (not in stdlib list but IS stdlib on Linux)
- `market` — local module (`scripts/market_filter.py`)
- `scripts` — local package (`scripts/__init__.py`)

All third-party deps (`scipy`, `numpy`, `pytest`) are documented in `requirements.txt`. No action needed.

---

## 🗑️ Elon Algorithm Results

4 agent output files scored ≥ 2/4. No file scored ≥ 4 (delete threshold).

| File | Score | Reasons | Recommendation |
|------|-------|---------|----------------|
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | 3/4 | No registry entry; not imported; 33d stale | KEEP — active research output with validated results |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | 3/4 | No registry entry; not imported; 33d stale | KEEP — pending Phase 2 research |
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | 2/4 | No registry entry; not imported | KEEP — passed validation, core research artifact |
| `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py` | 2/4 | No registry entry; not imported | KEEP — 3 days old, very recent |

All 4 files are standalone research scripts not designed to be imported — the "not referenced"
flag is expected, not a sign of dead code. Elon algorithm limitation: it cannot distinguish
"not imported because it's dead" from "not imported because it runs standalone". Context
overrides algorithm here.

---

## 🔍 Integration Contract Freshness

**PASSED.** All schema-critical commits in first-repo are covered by integration-contract.md.
Contract dates checked: 2026-05-07, 2026-05-14, 2026-05-20, 2026-05-25, 2026-05-29.
No `contract_stale` signal written.

---

## 🐛 Dead Code Detection

### trading-swarm
2 MONITOR candidates (not reaching ARCHIVE threshold yet):
- `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` — 33d, active research
- `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` — 33d, active research

`ci/validate_backtest.py` remains a recurring false positive (CLI tool, never imported). Confirmed active.

### first-repo — 44 MONITOR candidates
44 scripts in first-repo are 30-41 days old with no cross-file imports. This is consistent with
first-repo's design — most are standalone maintenance/diagnostic scripts. Notable candidates
worth eventual review:
- `paper_trading/` module (signal_generator, run_paper_trading, view_performance) — 41d, not imported
- `diagnose_activity_tracking.py`, `diagnose_deque.py` — 41d, one-shot diagnostic tools
- `monitoring/ai_analyzer.py`, `monitoring/ollama_client.py` — 41d, may be superseded

All at MONITOR level (not ARCHIVE) — none have hit 60+ days. Review when they cross 60 days.

---

## 📋 Proposals Requiring Oscar Approval

1. **Remove 5 merged worktrees** — commands listed above. Safe, branches are already on master.

2. **Investigate 3 unmerged worktrees** — especially `backtest-lh001-v4-20260522` (2 weeks unresolved):
   - If backtest work is superseded: `git worktree remove ... --force`
   - If valuable: merge to master before removing

3. **Rotate first-repo log files** — `daily_maintenance.log` at 98.8MB approaching 100MB limit.
   Manual rotation required for live monitoring system.

4. **Create `orchestrator/shared_utils.py`** — SQLite WAL, Telegram send, JSON load/save.
   Proposal unchanged from previous two weeks. Waiting on Phase 2 prep window.

5. **Tighten `print_credentials` security pattern** — 3rd consecutive week of false positive flood
   from first-repo (100+ hits, all benign). Narrower regex would improve scanner signal quality.
