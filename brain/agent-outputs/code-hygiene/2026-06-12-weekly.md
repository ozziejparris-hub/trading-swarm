# Code Hygiene Weekly Report — 2026-06-12

## Health Summary
Codebase: **NEEDS ATTENTION** (no critical security issues; 2 dependency gaps; deprecated module still called; stale registry entry)
Security issues: 0 critical | 0 high | 1 medium (test file, low risk)
Dead code candidates: 4 files (all "Monitor" — none safe to delete yet)
Duplicate patterns: 4 patterns across 2–54 files
Test coverage gaps: 4 agent output files (0 test files exist for any research outputs)
Orphaned worktrees: 0 (clean)
Log directory size: 7.9 MB (healthy)
Integration contract: **FRESH** ✅

---

## 🚨 Security Issues

### CRITICAL scanner alerts — all FALSE POSITIVES
The scanner flagged 3 "CRITICAL" patterns and 1 "Polymarket key" match. On inspection all are false positives:

- `first-repo/scripts/verify_system_observer.py:56,238` — `token='test_token'` — literal test placeholder, not a real credential
- `first-repo/scripts/run_system_observer.py:171` — `telegram_token = "test_token"` — same pattern, test placeholder
- `first-repo/scripts/archive/test_integration.py:95` — same test placeholder
- `first-repo/scripts/archive/polygon_wallet_lookup.py:44` — `0x3c499c542cef5e3811e1192ce70d8cc03d5c3359` is the well-known public USDC native token contract on Polygon, not a private key

**True security issues: 0**

### MEDIUM — Minor credential exposure risk (test file)
**File:** `first-repo/test_trend_analysis.py:25`
**Content:** `print(f"  Token: {telegram_token[:20]}...")`
**Risk:** Prints first 20 chars of `TELEGRAM_BOT_TOKEN` if set; this is a test script only.
**Fix:** Change to `print(f"  Token: [set]")` or remove the partial print. Low urgency — test file, not production.

---

## 🗑️ Elon Algorithm Results

Deletion candidates: **3 files** — all flagged ARCHIVE. **DO NOT ARCHIVE — see note.**

| File | Score | Reasons |
|------|-------|---------|
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | 3 | No registry match, not referenced, 42d stale |
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | 3 | No registry match, not referenced, 47d stale |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | 3 | No registry match, not referenced, 47d stale |

**⚠️ Override: RQ1.1 and RQ3.2 are Phase 5 Gate 4 assets.** The Elon Algorithm does not know about strategic gate requirements. These files must be retained until Phase 5 Gate 4 is resolved (passed or failed). RQ2.2 is supporting evidence for the elite signal thesis — also retain.

**Requires Oscar approval before any archival.** Earliest safe review: after Phase 5 Gate 4 decision.

---

## 🔄 Duplicate Code

Four patterns with HIGH consolidation value. None are active blocking issues — flagged as proposals.

### [HIGH] SQLite WAL Connection Setup — 54 files
Pattern: `sqlite3.connect` + `PRAGMA journal_mode=WAL`
Present in: 7 trading-swarm scripts/agent outputs + 47 first-repo files
Proposed consolidation: `orchestrator/shared_utils.py → get_db_connection()`
Priority: HIGH — every new agent reinvents this

### [HIGH] ELO Score Queries — 42 files
Pattern: `elo_score.*FROM traders` / `comprehensive_elo.*FROM traders` / `geo_elo.*FROM traders`
Present in: 2 trading-swarm + 40 first-repo files
Proposed consolidation: `orchestrator/shared_utils.py → get_trader_elo_scores()`

### [HIGH] JSON File Loading — 24 files
Pattern: `json.load(...)` with error handling
Present in: 8 trading-swarm + 16 first-repo files
Proposed consolidation: `orchestrator/shared_utils.py → load_json() / save_json()`

### [MEDIUM] Telegram Send — 4 files
Pattern: `api.telegram.org/sendMessage`
Present in: `run_feedback_loop_agent.py`, `polymarket_changelog_monitor.py`, `ollama_agent_loop.py`, `orchestrator.py`
Proposed consolidation: `orchestrator/shared_utils.py → send_telegram()`

**All consolidation proposals require Oscar approval before implementation.**

---

## 🪲 Known Issue — Deprecated Module Still Called

**`weighted_consensus_system.py` is deprecated but still imported in 3 live files:**

| File | Import |
|------|--------|
| `first-repo/scripts/run_analysis.py:72` | `from weighted_consensus_system import main as consensus_main` |
| `first-repo/analysis/consensus_divergence_detector.py:33` | `from weighted_consensus_system import WeightedConsensusSystem, ELORatingSystem` |
| `first-repo/analysis/market_confidence_meter.py:29` | `from weighted_consensus_system import WeightedConsensusSystem, ELORatingSystem` |

This was flagged in `brain/priorities.md` as a known hygiene task. The unified_elo_system.py supersedes it. The callers need to be migrated. **Requires Oscar decision** on whether to migrate now or defer to Phase 6 prep.

---

## 🗂️ Stale Registry Entry

`orchestrator/agent_registry.json` contains a task stuck in `"status": "respawning"` since 2026-06-04:
- Task ID: `signal-202606042140`
- Branch: `feat/signal-202606042140`
- Worktree: `/tmp/trading-swarm-worktrees/signal-202606042140` — **no longer exists**

This entry should be updated to `"status": "failed"` so the orchestrator can restart it or clear it. **Requires Oscar to update registry or restart the orchestrator task.**

---

## 🧪 Test Coverage Gaps

All 4 agent output Python files have zero test coverage. No test files exist for any of them.

| File | Functions Untested | Priority |
|------|--------------------|----------|
| `brain/agent-outputs/quant-research/RQ1.1/rq1_1_elo_persistence.py` | `connect_readonly`, `get_period_split`, `get_trader_elo`, `compute_brier_per_trader`, `run_pearson`, `classify_group`, `verdict`, `main` | HIGH (Phase 5 gate) |
| `brain/agent-outputs/quant-research/RQ3.2/rq3_2_crowd_vs_elite.py` | `load_data`, `collapse_to_implied`, `elo_weighted_consensus`, `analyse_market`, `summarise`, `main`, `print_market_table` | HIGH (Phase 5 gate) |
| `brain/agent-outputs/quant-research/GEO-ELO-003/geo_elo_oos_validation.py` | `connect`, `validate_contract`, `add_oos_column`, `compute_oos_elo`, `measure_oos_accuracy`, `get_oos_distribution`, `main`, `tier` | HIGH |
| `brain/agent-outputs/quant-research/RQ2.2/rq2_2_entry_timing.py` | `connect_readonly`, `load_legendary_trader_market_pairs`, `get_post_entry_price`, `analyse`, `print_summary`, `main`, `tier_stats`, `dir_stats` | MEDIUM |

Tests needed: `tests/test_rq1_1_elo_persistence.py`, `tests/test_rq3_2_crowd_vs_elite.py`, `tests/test_geo_elo_oos_validation.py`, `tests/test_rq2_2_entry_timing.py`

---

## 📦 Dependency Audit

Two undocumented third-party packages found in first-repo code (stdlib/local modules excluded):

| Package | Import | Used In | Missing From |
|---------|--------|---------|--------------|
| `apscheduler` | `from apscheduler.schedulers.asyncio import AsyncIOScheduler` | `monitoring/telegram_scheduler.py` | `first-repo/requirements.txt` |
| `psutil` | `import psutil` | `diagnose_activity_tracking.py`, `scripts/verify_system_observer.py`, `scripts/diagnose_database_locks.py` | `first-repo/requirements.txt` |

**Fix:** Add to `first-repo/requirements.txt`:
```
apscheduler>=3.10.0
psutil>=5.9.0
```
Low urgency — both are diagnostic/scheduling tools, not core trading logic.

---

## ✅ Maintenance Completed

- Orphaned worktrees: **0 found, 0 cleaned** (only current hygiene worktree active)
- Logs archived: **0** (all logs within 30 days, no file exceeds 100MB)
- Log file status:
  - `orchestrator.log`: 4.0 MB (largest — healthy, within tolerance)
  - `research_scout.log`: 55 KB
  - `backup.log`: 15 KB
  - All others: < 15 KB
- Integration contract: **FRESH** — all schema-critical commits since June 1 (unified_elo_system, update_research_exclusions, database) have corresponding entries in Section 8

---

## 📋 Proposals Requiring Approval

1. **shared_utils.py consolidation** — consolidate SQLite WAL, JSON load/save, Telegram send, ELO queries into `orchestrator/shared_utils.py`. Draft implementation available in task template. Estimated impact: removes ~50+ duplicate implementations across codebase.

2. **weighted_consensus_system.py migration** — update 3 callers (`run_analysis.py`, `consensus_divergence_detector.py`, `market_confidence_meter.py`) to use `unified_elo_system.py`. Phase timing is Oscar's call.

3. **Requirements fix** — add `apscheduler>=3.10.0` and `psutil>=5.9.0` to `first-repo/requirements.txt`. Low risk, purely additive.

4. **Registry cleanup** — mark `signal-202606042140` as `failed` in `orchestrator/agent_registry.json`. The STR-003 scan it was running (Fujimori Peru election) resolved; market has closed.

5. **Test files** — create `tests/test_rq1_1_elo_persistence.py` and `tests/test_rq3_2_crowd_vs_elite.py` as highest priority (Phase 5 gate scripts). Recommend delegating to quant-research-agent when it next runs those scripts.
